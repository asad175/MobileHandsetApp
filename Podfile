# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Mobile Handset App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Mobile Handset App
  pod 'Alamofire', '~> 5.2'
  pod 'XLPagerTabStrip', '~> 9.0'

  target 'Mobile Handset AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Mobile Handset AppUITests' do
    # Pods for testing
  end

end
